if not ("ARM" in config.root.targets):
    # We need support for ARM.
    config.unsupported = True
elif not ("arm" in config.root.host_triple):
    # We need to be running on an ARM host.
    config.unsupported = True
